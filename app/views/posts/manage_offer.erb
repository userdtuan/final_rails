<h2> All requests</h2>
<div class="row">
    <div class="col-lg-8">
    <!-- Nested row for non-featured blog posts-->
        <div class="row">
            <% @posts.each do |job| %>
                <div class="col-lg-6">
                    <%= render 'posts/postCard',{job: job} %>
                 </div>
        <%end %>
        </div>
    </div>
</div>
<h2> All offer</h2>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Price</th>
      <th scope="col">Status</th>
      <th scope="col">Action</th>

    </tr>
  </thead>
  <tbody>
    <% @offers.each do |offer| %> 
    <%# <%= render 'posts/post_in_table' ,post: post%> 
    <% post = offer.post%>

        <tr>
  <td><%= post.title%> </th>
  <td><%= post.starting_price%> </td>
  <td ><%=offer.status %></td>
  <td>
      <a class="btn btn-sm btn-primary" href="<%= post_path(post.id) %>">See Request</a>
  </td>
</tr>


  <% end %>
  </tbody>
</table>